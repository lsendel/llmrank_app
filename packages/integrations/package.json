{
  "name": "@llm-boost/integrations",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "format": "prettier --write src"
  },
  "dependencies": {
    "@llm-boost/shared": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3",
    "typescript": "^5.7",
    "vitest": "^3"
  }
}
